# Linux 容器应用检查

- 环境：`python:3.11-slim`，仓库只读目标逻辑在挂载目录运行。
- 命令：安装开发依赖后依次运行 Ruff、Pytest 和质量追踪检查。
- 结果：Ruff 通过；8 项 Pytest 通过；13 个指标覆盖 66 个需求编号。
- 已观察：POSIX 新会话、进程组 SIGINT、中断终态、SQLite WAL、配置文件原子替换。
- 范围：无 systemd 的 Linux 应用环境。
- 不包含：systemd 用户管理器、linger、主服务重启接管和目标机文件身份。
