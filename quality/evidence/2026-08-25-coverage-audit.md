# 最终覆盖差集审计

## L0 结论

- 注册需求：101 条。
- 质量指标：23 项，全部需求至少被一个指标映射。
- Windows 应用层：61/61 通过。
- Ubuntu 24.04/systemd 255 目标层：10/10 通过。
- Edge 浏览器：当前前端源码绑定收据通过；资源预算、桌面/窄屏几何、可访问性和关键交互均有直接断言。
- 本地候选代码：功能、交互、目标执行器与文档范围没有已知未处置高风险差集。
- 对外发布：用户明确要求暂不上传，GitHub Actions/Release 回执保持 `partial`，不能提升为已发布。
- 当前 29049 实例：前端制品与最新构建一致，最新后端已重启装载；温和停止示例退出码 0 且没有残留进程，历史日志保留，内联 API 冒烟成功，活动任务为 0。

## 覆盖维度

| 质量面 | 直接证据 | 不能证明 |
| --- | --- | --- |
| API/配置/插件 | HTTP 正反例、原子批次、幂等、配置 CRUD、所有内置插件可运行示例 | 未授权的远程网络部署 |
| 任务生命周期 | Ubuntu systemd PID 1、用户 transient unit、PTY、恢复与 cgroup 清理 | 任意第三方程序都愿意响应温和协议 |
| 权限 | 匿名 HTTP/SSE/WebSocket 拒绝、CSRF/HMAC、POSIX 权限 | 回环地址区分同机不可信 Unix 用户 |
| 网页交互 | Edge 语义状态、几何、剪贴板、响应式布局、axe、构建哈希 | 所有未来浏览器版本的像素一致性 |
| 性能资源 | 新根目录、零活跃任务时的服务/渲染器预算 | 用户命令自身的 CPU/RAM 上界 |
| 日志存储 | 实际文件、轮转、配额、WAL、及时字节偏移 | 用户命令在 LocalFlow 根目录外自行创建的文件 |

## Oracle 反证

- 删除唯一覆盖 `RQ-115` 的 `QM-016` 时，质量检查必须明确失败。
- 篡改浏览器源码哈希、删除关键交互断言或提高渲染堆至预算外时，质量检查必须失败。
- 旧的验证插件空输入 API 示例被固化为失败样本；新合同逐插件真实展开。
- Windows 上直接运行 Ubuntu 专用测试被分类为环境不等价，不允许冒充目标通过；真实 systemd 容器另行执行。

## 剩余动作

本地实现与运行实例无剩余重启动作。用户批准发布后，才运行 GitHub workflow 并取得静态 Release 外部回执。
