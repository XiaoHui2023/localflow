# 配置简化与检查门

本轮冻结三组可反证合同：新根目录/发布包只含两个生产插件及配置；`localflow.yaml` 独立于动态配置树且兼容迁移旧位置；运行界面通过插件检查合同显示解析后的只读事实。Python 门直接播种缺失工作目录、命令和 Case 目录，浏览器门比较通知出现前后的几何并检查只读状态组件。最终 Release 证据由同一提交的 CI、下载校验和与解压清单补齐。

自主学习依据采用 W3C Alert/Tooltip 模式、JSON Schema `readOnly` 注解和 Radix Toast 固定 viewport 契约。第三方 Skills 只完成发现，未安装：现有用户级网页设计 Skill 已覆盖职责，安装未经审计的候选不会增加交付证据。
