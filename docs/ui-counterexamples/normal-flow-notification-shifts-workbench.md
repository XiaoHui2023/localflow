# 把瞬时反馈放进工作区文档流

反例：保存后在编辑器上方插入一行“已保存”。消息本身很短，但它会改变工作区高度，使文件树、标题栏或 Monaco 重新布局；删除文字、改成绿色或缩小字号都没有解决所有权错误。

正确做法：可忽略的短反馈进入应用壳的固定通知层，错误类型决定 `status` 或 `alert`，不抢焦点并尊重减少动画。浏览器在触发前后比较文件树、工具栏与内容区矩形，位置和尺寸必须完全相同。冲突、删除确认和字段错误需要用户处理，继续由所属内容或对话框承载，不得伪装成会消失的 Toast。
