# LocalFlow 质量指标

机器清单位于 `quality/traceability.json`，逐项记录需求、负责人、自动测试、判定依据、故障样本、证据、状态和准确的表述范围。`tools/check_quality.py` 检查全部需求无遗漏、指标不重复、状态合法，并要求测试与证据路径真实存在；对应故障测试删除一项指标后必须失败。

## 当前状态

| 指标 | 范围 | 状态 | 主要证据 |
| --- | --- | --- | --- |
| QM-001 | 任务输入、详情、状态、快照 | passed | HTTP 与 SQLite |
| QM-002 | 筛选、稳定游标 | passed | 插页分页与非法游标测试 |
| QM-003 | 工作区、独立终端、悬浮确认、日志续读 | passed | Edge 新完成红点、仅悬浮确认、HTTP 控制、字节偏移日志 |
| QM-004 | systemd、恢复、交互停止、cgroup 清理 | passed | Ubuntu PID 1、孤儿 PID 消失、单元为空 |
| QM-005 | 互斥队列、批次、不可变快照与递增 seed | passed | 阻塞 ID、时间线、同秒多任务/重启递增、批次回滚与入队后零参数改写 |
| QM-006 | 配置、受限导入、分层诊断、原子写 | passed | 普通/公共/任务分类、越界导入、版本冲突与替换故障 |
| QM-007 | 插件、发现、声明式模板 | passed | 代次、诊断和任务快照 |
| QM-008 | 网页视觉、主题、队列压缩、seed 详情、渐进披露、窄屏 | passed | Edge 收据、仿真真实运行后的“随机种子”标签和值、同名 `×N`、标签折叠、三档详情几何与 axe 扫描 |
| QM-009 | API、OpenAPI、事件、并发幂等 | passed | 集成测试、并发同键单批次与生成描述 |
| QM-010 | 会话、CSRF、HMAC | passed | HTTP/WS 拒绝、持久会话与密钥轮换 |
| QM-011 | 时间校准、单调时长 | passed | 固定 helper、审计、墙钟故障样本 |
| QM-012 | 保留期、SQLite WAL | passed | 精确行和日志目录清单 |
| QM-013 | 发布闭环 | partial | 完整解压目录与工作流已实现；新版 Release 待 GitHub 实际运行 |
| QM-014 | 交互便利性与人性化 | passed | 常用路径动作预算、反馈不遮挡、错误就地显示与冗余控件缺席 |
| QM-015 | 日志可观测性与空间安全 | passed | 双轨轮转、敏感值遮盖、单任务/总量/SQLite/WAL 上限与故障样本 |
| QM-016 | LocalFlow 本体资源预算 | passed | 零活跃任务、服务进程树 RSS/CPU、Edge 渲染 CPU/堆/DOM/请求/连接直接测量 |
| QM-017 | 紧凑任务与步骤经济性 | passed | 任务信息删减、点击复制、独立终端、三档无溢出、配置直接操作计数 |
| QM-018 | 单请求配置式运行 | passed | 内联/已有配置共享诊断、无副作用预演、原子批次、幂等重试与错误输入拒绝 |
| QM-019 | 插件机器 API 合同 | passed | 每个内置插件的配置 schema、输入 schema、字段、预演与可运行示例逐项展开 |
| QM-020 | 配置 HTTP 生命周期 | passed | 创建、读取、条件保存、移动/重命名、删除与缺失复查 |
| QM-021 | HMAC 程序客户端完整能力 | passed | 每请求重签、完整任务/日志、HTTP 控制、配置 CRUD、插件合同和轮换后重读密钥 |
| QM-022 | 通用插件配置与 Case 字段映射 | passed | 完整合成 schema、公共字段冲突隔离、异名 Case 插件在 Edge 中实际展开两个任务 |
| QM-023 | 插件运行输入边界 | passed | 未声明输入 422、网页只提交插件字段、发现与运行正反例 |
| QM-024 | Agent 技能包与隐私 | passed | 五个标准技能通过结构校验、隐私扫描并进入解压包清单 |
| QM-025 | 零子命令发布入口 | passed | 默认双根、显式路径参数、未知子命令拒绝、冻结包真实任务闭环 |
| QM-026 | 局域网启动入口 | passed | 默认 wildcard 监听、私有 IPv4 展示、真实 Edge 局域网访问与可选 TLS 校验 |
| QM-027 | Ubuntu 浏览器兼容性 | passed | 最终 StaticX 二进制在最新版 Chrome/Firefox 完成全旅程，并在固定 Chrome 84/Firefox 78 完成启动、登录与 Monaco 旅程；启动和控制台错误为零 |
| QM-028 | 生产资产边界、简化首启与启动配置隔离 | passed | 新根目录精确清单、旧配置迁移、发布包生产项存在且测试项不存在 |
| QM-029 | 配置只读检查合同与刷新 | passed | API 正常/缺失路径故障、Edge 解析值与状态组件 |
| QM-030 | 瞬时反馈零布局位移 | passed | Edge 提示出现前后工具栏与资源树矩形完全一致 |
| QM-031 | 悬浮覆盖层与键盘语义 | passed | Edge 真实检查提示的 Portal、裁剪、碰撞、像素顶层、故障遮挡、焦点与 Escape 门 |
| QM-032 | 运行受理反馈与任务行稳定性 | passed | 延迟 HTTP 故障注入、按钮 submitting/accepted 状态、同一任务多阶段单元次序 |
| QM-033 | 任务日志与启动失败可诊断性 | passed | 提交即存在日志、真实 Hello World 文件/输出/生命周期、缺失目录失败的时间与日志闭环 |
| QM-034 | 字符串优先、列表兼容的命令合同 | passed | 规范化与反例测试 |
| QM-035 | 配置/插件统一资源工作区 | passed | CRUD、键盘旅程、冲突和事务回滚 |
| QM-036 | 软链接身份与边界 | passed | Linux 文件、目录、根链接和循环测试 |
| QM-037 | 文件双向即时同步 | passed | 原生事件加一秒校对、外部链接目标变更 |
| QM-038 | 终端流控与卸载生命周期 | passed | xterm 写入 ACK、切页和连接计数 |
| QM-039 | 控制器抗误关与无残留停机 | passed | 最终二进制信号、活动进程组与 PID 门禁 |
| QM-040 | 任意仿真命令、工作目录快照与左侧配置工作台 | passed | 自然失败基线、相对路径预演/快照、Make 依赖与正负副作用、三档响应式浏览器门 |
| QM-041 | 管理员网页安全退出 | passed | 权限、Alert Dialog、冻结制品真实退出与无残留门 |
| QM-042 | 终态终端历史、纯净字节流与成熟查找 | passed | 终态写入拒绝、只读 xterm 回放、零合成状态行、匹配计数与查找选项 |

## 交互质量预算

交互质量不是主观口号。浏览器门禁固定检查以下用户可观察结果：

- 可运行配置从资源树到可填写运行参数只需一次激活，不要求再切换“使用”模式。
- 无效配置从资源树到错误详情只需一次激活；资源树只给简洁状态提示，详细错误在打开后就地出现。
- 成功反馈位于固定通知层且在 5 秒内自动消失；出现前后资源树、工具栏和内容区的 `x/y/width/height` 完全不变。错误反馈保留，直到用户修正或切换上下文。
- 使用态显示插件合同返回的只读解析值；路径/命令错误同时具备非颜色图标、鼠标悬浮与键盘聚焦说明。打开、保存、外部同步和相关输入变化都会有界重查。
- 不设置手动刷新、手动扫描或重复的配置状态标题；文件变化和任务状态由自动同步负责。
- 状态不能只靠颜色表达：文件名保持中性，普通参数、共享片段、可运行配置和错误配置使用不同图标、可访问名称和行内诊断。
- 主导航只有任务、终端、设置；运行在任务页内以左侧选项卡组下方固定的“配置” disclosure 动作开合，按钮始终显示文字和方向图标、开合时不换位，插件与 API 说明只存在于项目文档。
- 终态任务继续出现在终端页并明确标为只读，保留 xterm 的 ANSI、选择、滚动和链接语义；成熟查找条提供 Ctrl/Cmd+F、匹配计数、前后定位、大小写、全词、正则与首尾跳转，写入控件和服务端写能力同时关闭。
- 运行面板在全新会话中默认展开，用户主动收起后在该会话保持收起；1440px 展开后位于任务左侧，1100px 以下置于任务上方，390px 内部资源树与配置面板上下排列。展开/收起只需一次操作，必须保留当前标签页的文件、模式和输入，并在全部三档视口保持零页面横向溢出。
- 当前文件名旁使用一个就近上下文操作组，编辑与运行都显示短标签和图标；不得把常用操作推到工作台远端或只用 Play 图标表达运行，界面不重复配置扩展名。
- 设置使用一个统一列表，标签与控件在同一基线；桌面和 390px 视口的时间校准不得无故断成两行。
- 网页退出从设置页只需一次直接点击和一次必要确认，界面税为零。确认框默认聚焦取消，Esc 恢复触发焦点，Portal 模态层不改变底层几何，确认期间只允许一次请求。
- 390px 视口无水平溢出，axe serious/critical 问题为 0。此门禁不外推到未测试的辅助技术、语言和任意视口。
- 有效配置的使用态只允许出现插件 `run_fields`；配置专属字段不得渗入日常运行表面。工作目录按“运行前解析值 → 日志冻结值 → 有异常时再查执行器/文件副作用”渐进披露，日常界面不堆叠 systemd 或 shell 细节。
- Case 网格首次进入与成功提交后次数必须全部为零；普通折叠或导航保留尚未提交的草稿。框选只改变临时作用域，Case 名、聚焦和滚轮不得隐式增加次数。具名增减按钮的短按严格一步，按住至少 550 ms 后才有界加速，释放后不得继续；零值不显示减少按钮。随机种子默认留空；正式入队后任务详情必须直接显示“随机种子”，且与不可变命令实际使用的值一致。
- 从运行页切换到其它导航再返回时，所选文件、编辑/运行模式和未提交的运行参数保持不变。
- 缺公共字段、字段类型错误和语法错误必须分别产生可见诊断，且运行 API 全部 fail closed 为 422。
- 一级按钮高度为 34–38px，资源树图标按钮为 32px；异常尺寸会使 Edge 几何断言失败。
- 已打开页面必须在构建修订变化后 8 秒内产生新主文档导航；HTML 与 API 的 `Cache-Control` 必须为 `no-store`。
- 普通任务行首点数量必须为 0；新完成任务恰有一个信息/品牌强调色圆点，且颜色不得等于危险色。聚焦、脚本展开和刷新保持圆点，只有真实鼠标悬浮后消失。
- 历史任务与带插件计算值的详情分别在 1440px、760px、390px 验证：末项至详情底边不超过 12px，任务卡片不存在行与详情之外的高度。仅验证桌面宽度不构成通过。
- 同标签集同名队列无条件合并为 `×N`；总队列第 20/21 项形成阈值对照，超过 20 才出现按完整标签集折叠，无标签项不折叠。

完整命令、环境、目标探针、浏览器截图、故障样本和工具兼容性说明见 `quality/evidence/2026-08-25-final.md`。

发布兼容性是硬门：PyInstaller 与 StaticX 必须在同一个 Ubuntu 16.04 基线内完成，CI 随后用 QEMU 在 `qemu64`、`core2duo`、`Opteron_G1` 三种无 AVX 的早期 x86-64 模型上执行最终制品的零参数启动探针。新宿主机原生 smoke 不能替代该门；只验证主体而让下游重新捆绑加载器也不能通过。
本次逃逸、成熟实现对照、失败迭代与声明边界记录在 `quality/evidence/2026-08-26-linux-isa-escape.md`。

## 本体资源预算

预算唯一来源是 `quality/resource-budgets.json`。服务样本先从 SQLite 证明活跃任务为零，再聚合控制器及其服务子进程；页面样本在完整运行工作台空闲六秒，使用 Edge DevTools 的累计任务时间差、JS 堆和 DOM 计数，同时统计 fetch/XHR 与存活 WebSocket。浏览器共享外壳不归属单个网页，因此不冒充 LocalFlow 页面内存；页面可归属开销由渲染进程指标表达。收据超限、缺字段、契约不一致或活跃任务非零均由 `tools/check_quality.py` 拒绝，测试还把堆值篡改为超限值证明判定器能抓住失败。

## 操作次数预算

从“对象已选中”开始计数。重命名需要一次直接点击后进入名称输入；删除需要一次直接点击和一次不可逆确认。打开通用“文件操作”菜单属于界面税，目标为零。任务值复制为一次点击，终端是独立能力所以允许一次一级导航，但任务展开区不得再要求“详情→终端”的二级往返。

## 发布判定

当前核心服务、Ubuntu systemd 执行、密钥初始化权限、生产前端构建与 Edge 浏览器交互均有直接证据。浏览器门禁每轮先构建当前源码，再验证匿名设置页秘钥登录、错误反馈、登录控件消失、持久会话、配置树 CRUD、配置内使用、任务原位详情、xterm、axe 与 390px。服务测试另行验证回环不自动授权、服务重启后会话保持、秘钥变更撤销旧会话，以及 POSIX 首建模式和用户改权后不检查、不修复、不重生。

局域网入口门禁验证默认 wildcard 监听只展示可复制的首选私有 IPv4、真实 Edge 经该地址访问、显式回环保持原样、可选 TLS 必须成对且代理 CIDR 必须有效。HTTP 仅声明覆盖可信离线局域网，不扩张为具备传输机密性。

任何自主学习或工具接入失败都必须在最终报告和证据文件中写明学习目标、可验证错误、影响、替代方案与恢复条件。

## 2026-09-07 新增门禁

| 指标 | 阻断条件 | 直接证据 |
| --- | --- | --- |
| QM-043 Case 通用步进器 | 出现独立加号、Case 名称/padding/数字区域不能增加、滚轮改值、显示零值计数面、普通点击跨多步、长按无延迟/不停止、运行后未清零 | Edge 公共入口行为测试与 `quality/operator-interaction-contract.json` |
| QM-044 自由配置工作区 | 出现 plugins/冗余根或文件分类、新建模板、语义诊断阻止保存、缺少拖放/键盘移动 | workspace API/仓储测试与 Edge 资源树旅程 |
| QM-045 大日志与插件审查 | xterm 注入状态文字、整份日志进浏览器、无区间/全局搜索、日志路径按控制器 cwd、未来日志缺失报错 | 终端 HTTP/WebSocket/搜索测试、验证插件预演与真实副作用测试 |
| QM-046 配置初态、脏保存与可扩展插件配置 | 无偏好时面板折叠、未修改仍可保存、修改后不可保存、保存成功仍可保存，或验证插件拒绝 `root`/`extra_inputs` | Edge disclosure/保存行为、插件诊断与 schema 测试、交互机器契约 |
| QM-047 仿真自定义文本可见性 | `custom_texts` 未进入 schema/预览/快照，变量未冻结，完成态丢失，列表被合并或任一行不能独立复制 | 插件展开与结果测试、配置检查 API、Edge 逐项复制旅程、交互机器契约 |
| QM-048 审查层级与多文件草稿 | 出现 Shell/命令入口或执行器包装、标签逗号拼接/可复制、普通项出现成功勾、复制行重复图标、缺失路径无插件声明或无叉号、Case/seed 选择改写模板文本、切换文件丢草稿/圆点或脏配置可提交 | 配置检查 API、任务显示命令、Edge 正常/缺失路径旅程、多文件编辑保存旅程、交互机器契约 |
| QM-049 精简仿真任务详情 | 验证任务详情出现单独的随机种子或运行日志字段，或隐藏操作误删 API 快照和结果判定值 | 插件快照测试与 Edge 验证任务详情反例 |
| QM-050 终端列表状态模型 | 条目带通用装饰图标或装饰点、运行中名称染色、重复状态/只读历史文字、宽屏列表小于 248px、活动/历史未分组或顺序反转、日志未增长产生新内容点、背景日志增长无点、选择后不清除 | Edge 真实后台输出增长旅程、名称/标签、可访问名称与分组几何断言、交互机器契约 |
| QM-051 Shell 启动文件与工作目录 | 任意插件仍需显式 Shell、未按 `$SHELL`/passwd 自适应、bashrc/cshrc 快捷指令不展开、argv 被隐式送入 Shell、rc 中 `cd` 使相对产物落到 LocalFlow 根 | 命令模型反例、Linux 隔离 HOME `.bashrc`/`.cshrc` 双副作用测试、systemd 真实任务门 |
| QM-052 进展感知停止与硬上限 | 正常清理因固定短超时被杀、旧日志误续期、持续刷日志无限阻止强杀、只杀主 PID、cgroup 未空即报终态 | 真实 systemd/PTY 正常慢退出与恶意持续输出双夹具、单元 inactive 断言 |
| QM-053 服务退出所有权与 Agent 终端 API | 结果观察异常把活进程标为 lost、强杀阶段被旧序列覆盖、控制器先于 cgroup 退出、终态仍可写、Ctrl+C 与任务停止混淆 | 等待通道故障注入、服务级完整 cgroup 清理、签名 HTTP/WebSocket 终端闭环 |
| QM-054 持久网页身份 | 启动时重写网页密钥、重启/端口变化要求重输、共享域允许 HTTP/IP/域外主机、把管理员密钥放浏览器存储 | AuthManager 重建、重启 cookie、RFC CookieJar 兄弟域与非法域反例 |
| QM-055 Agent 资源 API | 202 只有不可发现 ID、Location 与正文不一致、批量顺序丢失、幂等重试指向新资源、Agent 不能增量读/检索/输入/控制/resize/中断 | 任务/批次回执测试、签名客户端全链路与终态 409 |
| QM-056 Case 行按压状态机 | Case 行普通点击不是一步、长按未延迟渐进、减号冒泡加一、键盘多步、加号重新出现 | Edge 行点击/长按/分组/减到零旅程与交互机器契约 |
| QM-057 密钥权限一次性初始化 | 首建不是 `0700/0600`、后续启动审计/修复/拒绝用户权限、权限变化触发密钥重生、API 密钥轮换重置用户模式 | Linux 首建、改权重启、缺失补建与轮换模式反例 |
| QM-058 任务时间语义 | 开始时间直接显示冗长 ISO、缺失 `time[datetime]`、完整时区值不可发现或图标没有文字 | Edge 详情语义、可见短时间与悬浮完整时间断言 |
| QM-060 页面切换、列表几何与常用路径身份 | 终端切回任务重建工作台或超过 250 ms 双帧预算、终端离开后仍挂载、`code-list` 项缩入固定标签列、常用仍显示重名标题/标签或截断路径 | Edge DOM 身份与双 `requestAnimationFrame` 计时、列表 owner/row/value 几何、路径唯一可访问名称及无溢出断言；固定 Chrome 84/Firefox 78 验证保留/清理语义 |
| QM-061 调度容量、公平性与资源归属 | 默认固定四并发、忽略 affinity/cgroup quota、内部任务真相截断 500、阻塞前缀饿死独立任务、无界启动、把物理争用误报为控制器隔离 | 自动/显式容量单测、501 阻塞+独立任务有界扫描及时延预算、状态 API、Edge 控制器/浏览器资源收据、systemd 独立 cgroup 与 PSI 运维边界 |
| QM-062 终态任务详情结构 | 任意数组被 JSON 化/拼接为一行、元素不能独立复制、列表缩入标签列、开始时间使用孤立图标或缺少明确文字 | Edge 创建并等待真实任务完成后断言数组 `role=list/listitem` 数量、owner/row/value 宽度误差不超过 1px、逐项文本，以及“开始时间”文字和零 SVG |
| QM-063 初始兼容包资源 | Monaco/worker 进入普通页面初始包、入口 gzip 超过 0.3 MiB、收据大小与最终构建资产不符、编辑时未成功按需加载 | Vite 最终构建分包、Edge 编辑前后 Resource Timing、收据 bundle metric、`check_quality.py` 独立 gzip 复算及超预算故障注入 |
| QM-065 工作区复用与实例数据隔离 | 核心生成插件专属 `cases/`、默认把动态文件写到工作区/源码根、两个实例交叉读写历史、同一数据目录可重复启动、数据目录改变任务 cwd、systemd 单元跨实例冲突、损坏或未来状态破坏配置和密钥 | 无核心 Case 根断言、插件 Case 来源测试、双实例应用测试、实例锁、状态格式隔离/拒绝夹具、CLI 规范名/兼容名冲突测试、真实 systemd 与冻结制品双目录 smoke |
